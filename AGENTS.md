# This is my doom configuration

Doom source is at ~/.emacs.d and is currently in use.
My configuration is in init.el and config.org.
The org file is and org literate configuration.
packages.el and config.el are to be ignored as they are generated from config.org via tangling.

